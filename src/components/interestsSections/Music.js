import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import {
  MusicIcon,
} from "../../assets/icons/interestsIcons";
import { motion } from "framer-motion";

export default function Music(){
  return (
    <motion.div className="musicAnimation">
      <h4>Music</h4>
    </motion.div>
  );
}