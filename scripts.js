const earlyLife = document.getElementById('#earlyLife');
//function to change displays of the resume dropdown sections
const revealResumeSection = (section) => {
    if(section = earlyLife){
        earlyLife.style.display = 'inline-block';
    } 
}

